name: Security — Vulnerability Scan

on:
  schedule:
    - cron: '0 2 * * *'  # Daily at 2 AM UTC
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read
  security-events: write

jobs:
  # ─── Trivy Container & Filesystem Scan ───────────────────────────────────────
  trivy:
    name: Trivy Scan
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Run Trivy filesystem scan
      uses: aquasecurity/trivy-action@master
      with:
        scan-type: 'fs'
        scan-ref: '.'
        format: 'sarif'
        output: 'trivy-results.sarif'
        severity: 'CRITICAL,HIGH'

    - name: Upload Trivy results to GitHub Security tab
      uses: github/codeql-action/upload-sarif@v3
      if: always()
      with:
        sarif_file: 'trivy-results.sarif'
        category: 'trivy'

  # ─── .NET NuGet Vulnerability Audit ─────────────────────────────────────────
  dotnet-audit:
    name: .NET Dependency Audit
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v6
      with:
        dotnet-version: '9.0.x'

    - name: Cache NuGet packages
      uses: actions/cache@v4
      with:
        path: ~/.nuget/packages
        key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
        restore-keys: ${{ runner.os }}-nuget-

    # The legacy solution configuration does not select every project. Restore
    # each project so the audit has assets for every deployable module.
    - name: Restore all projects
      shell: bash
      run: |
        set -euo pipefail
        while IFS= read -r -d '' project; do
          dotnet restore "$project"
        done < <(find src -name '*.csproj' -print0)

    - name: Audit NuGet packages for known vulnerabilities
      run: dotnet list src/Bank.sln package --vulnerable --include-transitive
